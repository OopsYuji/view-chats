PGHOST=localhost
PGPORT=5432
PGDATABASE=chats
PGUSER=postgres
PGPASSWORD=supersecret
PGSSLMODE=disable
PGSSLREJECTUNAUTHORIZED=false

# Optional table override (defaults to chat_messages)
CHAT_TABLE=chat_messages

# Comma separated list of origins that can access the API. Leave empty for all origins.
CORS_ORIGIN=http://localhost:5173,http://localhost:3000
PORT=4000
